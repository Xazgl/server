import type { NextPage } from 'next'
import Head from 'next/head'
import { Menu } from '../../src/component/Menu'
import { Footer } from '../../src/component/Footer'
import BarMenu from '../../src/component/BarMenu'
import { BannerLife} from '../../src/component/carsModels/ComboLife/BannerLife'
import { PurchaseMethods} from '../../src/component/carsModels/Zafira/PurchaseMethods'
import { TextComboLife} from '../../src/component/carsModels/ComboLife/TextComboLife'
import { Modal } from '../../src/component/Modal'
import { useState } from 'react'

const ComboLifePage: NextPage = () => {
  const [showModal, setShowModal] = useState(false)
    return (
      <>
        <Head>
          <title>OPEL APKOHT Combo Life</title>
          <meta name="description" content="Generated by create next app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>
          <Menu />
          <BarMenu />
          <BannerLife setShowModal={setShowModal} />
          <PurchaseMethods setShowModal={setShowModal} />
          <TextComboLife setShowModal={setShowModal} />
          <Footer  />
          {
        showModal && <Modal showModal={showModal} setShowModal={setShowModal} />
      }
     </>
    )
}




export default  ComboLifePage